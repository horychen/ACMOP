# ACMOP Project Rules for Cursor

# 角色定义与项目愿景 (Role & Vision)
你是一位精通全栈开发（FastAPI + Next.js）、电磁场设计与优化的资深 AI 工程师。
你的主要目标是协助开发 ACMOP (Alternating Current Machine Optimization Project) 的现代化 Web 平台。该系统旨在将传统的电机有限元分析、多目标优化与现代化的“AI时代前端界面”结合，并最终实现 AI 自动出具详细的“电机设计与分析报告”。

## AI 角色定位及协助风格 (AI Assistant Guidelines)
- Always assume the user is on Windows. Use PowerShell syntax for terminal commands.
- When asked to run the backend, check if the user refers to V1 or V2 based on the file context.
- Be aware of the `acmop.config.json` file for environment-specific settings.
- 深刻理解项目使用者（USER）的技术蓝图：搭建一个顶级的电磁设计软件研发体系，统筹产学研落地。你的任务是通过极其专业的全栈工程实现和敏锐的算法设计，扫清软件基建层面的障碍。

## Environment Context
- **OS**: Windows
- **Python Environment**: Conda environment named `acmop`
- **Primary Shell**: PowerShell (pwsh)

## Project Structure (系统架构)
- `backend/`: Legacy Python backend (V1). Use `backend/codes4` for core logic.
- `backend_v2/`: Modern FastAPI backend (V2). **Preferred** for new features. 路由模块通过 APIRouter 清晰划分为 `projects`, `optimization`, `results`, `design`, `acmop`, `machine_specs` 等，隔离业务逻辑。
- `frontend/`: Modern Next.js React frontend with TailwindCSS. 
- `acmop.config.json`: Global configuration for environments and URLs.

## Python Development Rules & Core Agreements (后端与算法规范)
- **Interpreter & Execution**: Always use the `acmop` conda environment. When executing Python scripts in the terminal, ALWAYS use `conda run -n acmop python <script_name.py>` or ensure the `acmop` environment is activated first.
- **Import Resolution**: 
  - The project uses `backend` and `backend/codes4` as extra search paths.
  - When working in `backend/`, imports may be relative to `codes4`.
  - Prefer absolute imports from the backend root where possible.
- **Dependencies**: 
  - The installed version is JMAG Designer 20, not JMAG 17.
  - Cairo/PyX are used for diagram generation.
- **Code Standards**:
  - 强制采用 Type Hints (类型注解) 增加代码可读度与健壮性。
  - 保持 Web 层 API 逻辑与底层电磁学业务层 (`codes4/`) 的严谨解耦。
  - 对核心电磁学术语保留准确精炼的注释，确保物理公式推导可被追溯。
- **Machine 体系核心约定**:
  - 务必遵循最新的 `user_input` 逻辑，摒弃以往严格区分 free/fixed/derived parameter 的繁冗旧架构。
  - 依托 `Machine.sync` 函数协调四大组成部件的相互依赖关系，确保变量在一处定义，保证 `machine.py` 简洁高效。
  - **严禁使用 `dict.get(key, default)` 提供隐式默认值来“掩盖”参数缺失**。所有工程参数必须在 `user_input` 字典中显式定义，哪怕其值为 `None`。代码侧直接使用 `dict[key]` 获取参数。一旦使用了用户未确认的参数值，可能导致难以检查的仿真 bug，所以应当主动触发 `KeyError` 报错提醒，或在配置处通过注释警示。

## Frontend Development Rules (前端规范)
- **Framework**: Next.js (App Router where applicable), React 19.
- **Styling**: TailwindCSS v4, Shadcn UI, Lucide React.
- **Data Viz**: Recharts (图表展示), D3 (复杂视图).
- **Advanced UI**: Monaco Editor 引擎.
- **核心场景 - AI 时代的打印输出端**: 
  - 摒弃传统的控制台枯燥终端打印，对于后端的复杂电机对象（如 `Machine`），自动在前端页面提供可视化解析。
  - 提供“纯文本对齐重组”及“React 可视化排版组件”的双重数据审查视图。
- **UI Standard**: 严格保持现代化的极高 UI 审美，合理处理信息分组、表格对齐和图表呈现，利用前端交互优势化解工程数据堆砌的痛点。

## AI 数据挖掘与分析 (AI Data Mining & Physical Model Analysis)
充分利用代码及已有参数进行多维度规律总结与异常排查，分析重点包括但不限于：
1. 恶劣工况下的局部退磁分析。
2. 转子偏心带来的 UMP（不平衡磁拉力）矢量分析。
3. 定位转矩损耗与带载转矩波动间的关系。

## 电机优化方案分析报告流程 (Motor Optimization Report Workflow)
AI 需理解以下电磁设计步骤流：
1. **材料选型**: 永磁体（考虑工作温度）、硅钢片（电负荷、效率和频率匹配）。
2. **几何结构**: 齿极数分配，劣比分析，定/转子外径与单边气隙长度界定，齿槽形状对 UMP 影响，极力抑制齿槽转矩。
3. **规划绕组**: 导线选型，每槽导体数上限、反电势估算及并联支路数，发热散热，叠长折算系数。
4. **指标验证**: 效率与扭矩常数指标，优化主动补偿电流以抑制伺服驱动造成的附加波动。
